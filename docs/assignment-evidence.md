# Assignment evidence map

This map records what the repository proves as of 23 September 2026, at `main` `716265d`. Code evidence means an implementation exists on `main`. It does not establish production deployment, correct configuration, successful use by real users, or measured product outcomes. Do not turn a proposed metric or a local code path into a result claim. For the general pre-submission checklist, see `docs/release-readiness.md`.

| Milestone | Evidence in this repository | Status and evidence still needed |
| --- | --- | --- |
| M0 Problem definition | `docs/milestones-report.md`, problem statement and target segment | Product hypothesis is written. The draft cohort of 20 students and all other user-research claims require dated notes, consented method and results before inclusion. |
| M1 Competitive landscape | Report's sourced comparison of Whering, Acloset and Alta; `docs/competitor-onboarding-review.md` contains analysis of supplied screenshots | Feature descriptions cite current official product pages. Do not publish unverified pricing, user totals, market share, Singapore ranking or claims that a competitor lacks a feature. Add access dates or recheck before final PDF. |
| M2 Product vision and user stories | `README.md`, app routes, Supabase schema and report | Core signed-in wardrobe-to-outfit and purchase-check user stories are represented in code. Confirm each journey against production before calling it complete. |
| M3 Defensibility | Per-user wardrobe, outfit requests, feedback and purchase records in `app/supabase/schema.sql`; `app/src/lib/outfit-feedback.ts` | The outfit route now reads stored wore/liked/rejected feedback and saved outfits back into its prompt as a per-item score (`loadFeedbackContext` in `app/src/lib/outfits/server.ts`, called from `app/src/app/api/outfits/route.ts`). This is a transparent weighting rule, not a trained model, and no user study yet shows it improves outcomes, but the feedback loop described as a future gap in earlier drafts is now shipped. |
| M4 Users and acquisition | Product segment and channel hypotheses in report | No acquisition campaign, channel results, activated-user count or CAC evidence is present. Add only actual activity and observed data. |
| M5 MVP scope | Current routes and components; product requirements in `AGENTS.md` | Wardrobe, outfit planner, purchase evaluator and curated Explore route exist in code. Sizing (`chian/sizing-feature`, PR #10), the daily outfit feed, saved outfits and the "My Style" colour and archetype page have since merged into `main`. There is still no social inspiration feed and no brand-catalogue size recommendation beyond the shipped chart matcher and screenshot checker. |
| M6 Monetisation | Landing page `app/src/app/page.tsx`; `app/src/lib/account-entitlements.ts` | Landing copy offers a free beta and labels S$8.90/month Plus as planned and unavailable. The code now has a free/premium account tier and per-feature limits (Beautify edit credits, Explore refresh, outfit AI credits), but nothing in the codebase sets an account to premium, so there is still no payment or checkout flow. Unit-economics values in the submitted draft are assumptions, not observed metrics. |
| M7 LLM role | `/api/items/analyze`, `/api/outfits`, `/api/purchases/evaluate`, `/api/explore`, `/api/sizing/extract`, `/api/style/archetypes`, `/api/daily-outfits` | Multimodal extraction, outfit selection, purchase explanation, feed ranking, sizing-chart screenshot reading, style-archetype grouping and daily-outfit picking exist in server routes. The first three and sizing use the paid project; do not run any of them with a real key during agent verification. |
| M8 Prompt design | Real prompt strings in those API routes and `docs/milestones-report.md` | Report quotes representative current prompts. Prompts are inline, not separately versioned. No prompt iteration log or before/after evaluation is present. |
| M9 Model and provider | `app/src/lib/ai/gemini.ts`, `app/package.json` | Google Gemini through Vercel AI SDK is selected. `GEMINI_MODEL` overrides default `gemini-3.6-flash`; no explicit temperature, top-p or token limit is configured in calls. No local head-to-head benchmark exists. |
| M10 AI interaction patterns | Zod schemas; user-scoped route queries; NEA forecast helper; purchase similarity function; `app/src/lib/outfits/daily-rules.ts` | Structured generation, multimodal input, controlled context, schema validation and deterministic purchase similarity are implemented. Outfit ranking still uses the full confirmed wardrobe, not a deterministic role/weather pre-filter, but it now folds in a feedback score per item. Explore uses a user-specific cached feed over a team-curated catalogue. The daily outfit feed tries deterministic rules first and only calls the free-tier model to refine the pick, a concrete instance of using a smaller or no model for the simple case. |
| M11 Evaluation | Proposed case list in `AGENTS.md`; `app/scripts/eval-sizing.mjs` and `app/tests/fixtures/sizing/README.md` | A runnable evaluation harness for the sizing screenshot checker exists, gated behind `SIZING_EVAL_I_AM_HUMAN` so only a person triggers the paid call, but the fixture screenshots in it are still placeholders. No evaluation dataset, run log, human ratings or measured results exist yet for clothing extraction, outfit quality or purchase judgments. The report states this directly and gives a protocol for a future run. |
| M12 Production optimization | `app/src/lib/weather.ts`, AI route limits and selected prompt fields, `app/src/app/api/style/archetypes/route.ts`, `app/src/app/api/daily-outfits/route.ts` | Forecast fetches use 900-second Next.js revalidation. AI calls have per-process in-memory limits. Style archetypes and the daily outfit feed cache their result per user and only call the model when the wardrobe or the day changes. No cache-hit, latency, cost or before/after measurements are available. Limits do not coordinate between serverless instances. |
| M13 Safety and security | `app/supabase/schema.sql`, server routes, file checks, `UNTRUSTED_CONTENT_RULE`, response schemas and signed image URLs | Code includes RLS ownership, private bucket policies, auth checks, upload MIME/size checks, schema parsing, returned-ID filtering, prompt-injection instruction and rate limits. No adversarial or cross-account runtime test results are included. Verify production policies and provider settings. |
| M14 Name and logo | `design.md`, `app/src/app/icon.svg`, landing wordmark and generated OG image | Wearabouts is the selected name. The logo adapts the supplied W/hanger artwork. Record the original reference file/creator and permission or license in the final report; do not claim original authorship. |
| M15 Stack | `app/package.json`, `app/src`, `app/supabase/schema.sql`, `README.md` | Next.js 16, React 19, TypeScript, Supabase, Google Gemini via AI SDK, NEA data.gov.sg, and Vercel instrumentation are present. Production hosting and Supabase project configuration still need live verification. |
| M16 Workflows | Wardrobe upload/editor, planner, purchase evaluator | Three workflows are documented in the report. Attach screenshots or a production demo and record failures before final submission. |
| M17 AI UI | `attribute-fields.tsx`, upload progress, planner explanations and feedback controls, evaluator | UI supports correction, uncertainty, reasons and feedback. Save annotated screenshots from the deployed version. |
| M18 Landing, SEO and social sharing | `page.tsx`, `layout.tsx`, `opengraph-image.tsx`, `twitter-image.tsx`, `sitemap.ts`, `robots.ts`, `docs/launch-setup.md` | Local code now assigns the canonical only to the homepage and prefers `NEXT_PUBLIC_SITE_URL`, then Vercel's production domain. The supplied public URL is `https://wearabouts-zeta.vercel.app`. The production response previously inspected still exposed the older `drape-zeta.vercel.app` origin. Set the production environment variable and redeploy, then capture the live metadata and external preview. |
| M19 Analytics | `@vercel/analytics`, `@vercel/speed-insights`, `components/vercel-analytics.tsx`, `lib/analytics.ts`, consent UI | Vercel Analytics and Speed Insights are mounted. GA4 is optional and consent-gated. No Vercel or GA dashboard screenshot/report was supplied; report must wait for real deployed data. |
| M20 Product Hunt | `docs/launch-campaign.md`, `app/public/launch/product-hunt-cover.svg`, and M20 in `docs/milestones-report.md` | A draft tagline, description, maker comment, branded cover graphic, gallery capture plan and launch-day sequence are ready. Real interface screenshots, a demo video, launch date and launch results are not available. Keep the materials as drafts until the production URL and screenshots are verified. |

## Before export

- Group 2, production URL `https://wearabouts-zeta.vercel.app` and public GitHub URL `https://github.com/joojaja/CS3216-A3-Group2` are filled in. Add team names, matriculation numbers and accurate contribution summaries.
- Ask the Vercel project owner to set `NEXT_PUBLIC_SITE_URL=https://wearabouts-zeta.vercel.app` for Production and redeploy. Then verify the metadata, external preview and app with a non-sensitive demo account. Never test paid Gemini routes with a real key as an agent.
- Add actual screenshots for M16 to M19, and record dates and context.
- Run and document an evaluation before claiming M11 results; measure before claiming M12 improvements.
- Capture analytics only after meaningful use. Explain small sample sizes and do not present event counts as unique-user conversion rates.
- Confirm the source and rights for supplied landing images, fonts and logo artwork.
- Confirm the final branch/commit before export. As of 23 September 2026, all feature branches (including sizing, PR #10) are merged into `main` and there are no outstanding pull requests.
- Export the milestones PDF and the separate pitch PDF, then check page layout and links.
