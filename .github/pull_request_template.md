## What changed

## How it was tested

- [ ] `npm run lint`, `npx tsc --noEmit`, `npm test` and `npm run build` pass in `app/`
- [ ] Checked on a phone-width screen if the change touches the UI
- [ ] No call to a billed Gemini route was made while testing
